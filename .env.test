# Test Environment Configuration
# Use a local PostgreSQL for testing or a test database on Neon

# For local PostgreSQL (if available)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_fisio_rag
DB_SCHEMA=public

# For Neon test database (use the same as production for now)
# DATABASE_URL=postgresql://neondb_owner:npg_lD8Y3cotqMHP@ep-proud-surf-abw5697q-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# DB_SCHEMA=test_schema

# Neo4j Configuration (can be mocked for tests)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=test

# LLM Configuration (use a dummy key for tests)
LLM_API_KEY=sk-test-dummy-key
LLM_MODEL=gpt-3.5-turbo

# Other test configurations
JWT_SECRET_KEY=test-secret-key-for-jwt
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30
